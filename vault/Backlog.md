# Backlog

Source of truth lives in the repo as [`BACKLOG.md`](../BACKLOG.md) — that's
where new items get captured. This note exists so vault search finds it.

Headline items, as of 2026-05-27:

- Reshape into a **wiki-style home** + a separate **/explore tiled algos page**
- Kill the dense top-bar nav
- Audit jargon across pages (e.g. BM25 page references TF-IDF as a predecessor
  but TF-IDF now lives under Foundational)
- Centralize algorithm metadata in one config
- Glossary / hover-tooltip system
- Bigger HNSW corpus mode (30 nodes is too small to show "approximate")
- Compare-mode widget across algorithms
- Railway deploy ([[Deployment]])

See repo `BACKLOG.md` for the full list and rationale.
